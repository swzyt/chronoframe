export default defineNuxtRouteMiddleware(() => {
  // Public pages render the configured preview. API responses and media routes
  // enforce the preview limit, while /access unlocks the complete collection.
})
