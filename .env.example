# Admin user email (required)
CFRAME_ADMIN_EMAIL=
# Admin user name (default to Chronoframe, optional)
CFRAME_ADMIN_NAME=
# Admin user password (default to CF1234@!, optional)
CFRAME_ADMIN_PASSWORD=

# Site configuration
NUXT_PUBLIC_APP_TITLE=ChronoFrame
NUXT_PUBLIC_APP_SLOGAN=Mems rest within the lens.
NUXT_PUBLIC_APP_AUTHOR=
NUXT_PUBLIC_APP_AVATAR_URL=
# Color mode preference: light, dark, or system
NUXT_PUBLIC_COLOR_MODE_PREFERENCE=light

# Map Provider (mapbox/maplibre)
NUXT_PUBLIC_MAP_PROVIDER=maplibre
# Map style URL
NUXT_PUBLIC_MAP_MAPLIBRE_STYLE=
NUXT_PUBLIC_MAP_MAPBOX_STYLE=
# Mapbox access token for map features, Mapbox GL JS (Client-side, public)
NUXT_PUBLIC_MAPBOX_ACCESS_TOKEN=

# Mapbox secret access token for server-side, Mapbox Search API (Reverse Geocoding)
NUXT_MAPBOX_ACCESS_TOKEN=

# Custom Nominatim API base URL (use MapBox or proxy when official API is blocked in mainland China)
NUXT_NOMINATIM_BASE_URL=

# Storage provider (s3/local/openlist)
NUXT_STORAGE_PROVIDER=s3
# S3 storage provider configuration
NUXT_PROVIDER_S3_ENDPOINT=
NUXT_PROVIDER_S3_BUCKET=
NUXT_PROVIDER_S3_REGION=auto
NUXT_PROVIDER_S3_ACCESS_KEY_ID=
NUXT_PROVIDER_S3_SECRET_ACCESS_KEY=
NUXT_PROVIDER_S3_PREFIX=photos/
NUXT_PROVIDER_S3_CDN_URL=
NUXT_PROVIDER_S3_FORCE_PATH_STYLE=false

# Local storage provider configuration
NUXT_PROVIDER_LOCAL_PATH=./data/storage

# external access base URL for local storage, e.g. /storage (built-in /storage route)
NUXT_PROVIDER_LOCAL_BASE_URL=/storage

# OpenList storage provider configuration
# OpenList base URL, e.g. https://your-openlist-server.com.
NUXT_PROVIDER_OPENLIST_BASE_URL=
# OpenList root path, e.g. /115pan/chronoframe/photos
NUXT_PROVIDER_OPENLIST_ROOT_PATH=
# Authentication - Token authentication (recommended)
NUXT_PROVIDER_OPENLIST_TOKEN=
NUXT_PROVIDER_OPENLIST_ENDPOINT_UPLOAD=/api/fs/put
NUXT_PROVIDER_OPENLIST_ENDPOINT_DOWNLOAD=
NUXT_PROVIDER_OPENLIST_ENDPOINT_LIST=
NUXT_PROVIDER_OPENLIST_ENDPOINT_DELETE=/api/fs/remove
NUXT_PROVIDER_OPENLIST_ENDPOINT_META=/api/fs/get
NUXT_PROVIDER_OPENLIST_PATH_FIELD=path
NUXT_PROVIDER_OPENLIST_CDN_URL=

# Session password for encrypting session data, 32 characters long
NUXT_SESSION_PASSWORD=
# Optional dedicated secret for shared login/access rate-limit keys.
# When unset, Node and Go derive the rate-limit HMAC key from NUXT_SESSION_PASSWORD.
CFRAME_RATE_LIMIT_SECRET=
# Secret key for stable signing og images
# Use: npx nuxt-og-image generate-secret
NUXT_OG_IMAGE_SECRET=

# Database file path (SQLite). Docker default: /app/data/app.sqlite3
# Database email backups are configured in Dashboard -> Settings -> System -> Database Backup.
DATABASE_URL=./data/app.sqlite3

# Internal Go backend upstream used by the Node gateway when
# Settings -> System -> Backend -> Read provider is set to Go.
# Keep this address private; it is not exposed to browsers.
CFRAME_GO_UPSTREAM=http://127.0.0.1:8080

# Runtime actor ownership. Keep exactly one owner for each singleton actor.
# Node remains the safe default; use deploy/dual override files for local Go
# learning slices. When shared Redis is configured, pipeline consumers also
# acquire cf:v1:<env>:lease:pipeline-consumer before starting workers. Go can
# run the same Drizzle SQL ledger as a one-shot migrator only when
# CFRAME_DB_MIGRATOR=go and CFRAME_GO_MIGRATE_ONLY=true; normal API processes
# should keep migration disabled unless an explicit deployment job owns it.
CFRAME_DB_MIGRATOR=node
CFRAME_GO_MIGRATE_ONLY=false
CFRAME_PIPELINE_CONSUMER=node
CFRAME_BACKUP_SCHEDULER=node

# Go pipeline consumer learning settings. The current Go consumer safely handles
# photo, live-photo-video, video, photo-reverse-geocoding, and
# photo-erase-location tasks. Synthetic HEIC conversion is covered by the Go
# runtime image. Shared Redis runtime lease prevents accidental dual startup;
# production handoff, task-row fencing, and broader real-media corpus validation
# are still required before promoting the consumer owner as stable.
CFRAME_GO_PIPELINE_WORKER_COUNT=1
CFRAME_GO_PIPELINE_POLL_INTERVAL=3s

# Go readiness media-tool preflight. Keep enabled for normal Go backend runs so
# /health/ready can fail before routing media processing to a container that is
# missing exiftool/ImageMagick/libvips/FFmpeg dependencies. Set to false only
# for minimal JSON-only experiments or tests.
CFRAME_GO_MEDIA_TOOL_PREFLIGHT=true

# Go backup scheduler learning setting. The normal default is 1m; the
# deploy/dual Go scheduler verifier override lowers it for fast local feedback.
CFRAME_GO_BACKUP_SCHEDULE_REFRESH_INTERVAL=1m

# GitHub OAuth app credentials
NUXT_PUBLIC_OAUTH_GITHUB_ENABLED=false
NUXT_OAUTH_GITHUB_CLIENT_ID=
NUXT_OAUTH_GITHUB_CLIENT_SECRET=

# Optional insecure override. HTTP is detected automatically; enabling this
# also removes Secure from cookies served over HTTPS.
# NUXT_ALLOW_INSECURE_COOKIE=false

# Google Analytics Measurement ID (e.g. G-XXXXXXX)
NUXT_PUBLIC_GTAG_ID=
# Matomo Analytics configuration
NUXT_PUBLIC_ANALYTICS_MATOMO_ENABLED=false
NUXT_PUBLIC_ANALYTICS_MATOMO_URL=
NUXT_PUBLIC_ANALYTICS_MATOMO_SITE_ID=

# -----------------
# Upload Configuration
# -----------------
# Enable MIME type whitelist validation for file uploads (default: true)
NUXT_UPLOAD_MIME_WHITELIST_ENABLED=true

# Comma-separated list of allowed MIME types for uploads
# Default includes: image/jpeg, image/png, image/webp, image/gif, image/bmp,
#                   image/tiff, image/heic, image/heif, video/quicktime, video/mp4
NUXT_UPLOAD_MIME_WHITELIST=image/jpeg,image/png,image/webp,image/gif,image/bmp,image/tiff,image/heic,image/heif,video/quicktime,video/mp4

# To disable whitelist and allow any file type, set:
# NUXT_UPLOAD_MIME_WHITELIST_ENABLED=false


# Duplicate file handling has been moved to System Settings page.
# Configure it in Dashboard -> Settings -> System:
# - Detect duplicate files on upload (enabled/disabled)
# - Duplicate file handling mode (skip/warn/block)
